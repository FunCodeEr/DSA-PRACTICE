#include<bits/stdc++.h>
using namespace std;
char upper(char c){
	return 'A' + (c - 'a');
}
int main(){
	int i = 0;
	while(true){
		string s;
	    cin>>s;
		if(s.size() == 0){
			break;
		}
		else{
			for (i=0; i<=s.size(); ++i){
				s[i] = upper(s[i]);
			}
		}
		cout<<s<<endl;
	}
}
